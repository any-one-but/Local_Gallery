{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the gallery and settings windows.",
  "windows": ["main", "settings"],
  "permissions": ["core:default"]
}
